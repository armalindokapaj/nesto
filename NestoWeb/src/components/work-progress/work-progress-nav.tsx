import Link from "next/link";
import { LayoutDashboard, Layers3, GanttChart, ClipboardPen, AlertTriangle, Images, FileCheck2 } from "lucide-react";
import { cn } from "@/lib/utils";
const ITEMS = [["Overview","/dashboard/work-progress",LayoutDashboard],["Packages","/dashboard/work-progress/packages",Layers3],["Schedule","/dashboard/work-progress/schedule",GanttChart],["Daily reports","/dashboard/work-progress/daily-reports",ClipboardPen],["Control","/dashboard/work-progress/control",AlertTriangle],["Evidence","/dashboard/work-progress/evidence",Images],["Measurement","/dashboard/work-progress/measurement",FileCheck2]] as const;
export function WorkProgressNav({ active }: { active: string }) { return <div className="overflow-x-auto rounded-xl border border-border bg-surface p-1.5"><nav aria-label="Work progress sections" className="flex min-w-max gap-1">{ITEMS.map(([label,href,Icon]) => <Link key={href} href={href} className={cn("inline-flex h-8 items-center gap-1.5 rounded-lg px-3 text-xs font-medium",active===label.toLowerCase().replace(" ","-")?"bg-ink text-white":"text-ink-muted hover:bg-surface-sunken hover:text-ink")}><Icon size={13}/>{label}</Link>)}</nav></div>; }
export function WorkProgressHeader({ title, description, actions }: { title: string; description: string; actions?: React.ReactNode }) { return <div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[0.65rem] font-semibold uppercase tracking-[0.16em] text-gold">Work progress</p><h1 className="mt-1 text-2xl font-semibold tracking-tight text-ink">{title}</h1><p className="mt-1 max-w-3xl text-sm text-ink-muted">{description}</p></div>{actions&&<div>{actions}</div>}</div>; }
