import{redirect}from"next/navigation";import{getCurrentUser}from"@/lib/dal";import{can}from"@/lib/permissions";import{listAssetWorkOrders,listAssetsModule}from"@/server/assets-module";import{AssetsHeader,AssetsNav}from"@/components/assets/assets-nav";import{CreateWorkOrderDialog}from"@/components/assets/asset-dialogs";import{Card,CardContent}from"@/components/ui/card";import{Table,THead,TBody,TRow,TH,TD}from"@/components/ui/table";import{Badge}from"@/components/ui/badge";import{formatCurrency,formatDate}from"@/lib/utils";
export default async function Page(){const{tenantId,role}=await getCurrentUser();if(!can(role,"PROJECTS","READ"))redirect("/dashboard/executive");const[orders,assets]=await Promise.all([listAssetWorkOrders(tenantId),listAssetsModule(tenantId)]);return <div className="space-y-6"><AssetsHeader title="Maintenance work orders" description="Preventive, corrective and emergency work with SLA, technician, labor, materials, downtime, checklist and controlled lifecycle." actions={can(role,"PROJECTS","WRITE")?<CreateWorkOrderDialog assets={assets}/>:undefined}/><AssetsNav active="work-orders"/><Card><CardContent className="p-0"><Table><THead><TRow><TH>Work order</TH><TH>Asset</TH><TH>Priority</TH><TH>Schedule / SLA</TH><TH>Technician</TH><TH>Cost</TH><TH>Downtime</TH><TH>Status</TH></TRow></THead><TBody>{orders.map(o=><TRow key={o.id}><TD><p className="font-medium text-ink">{o.number}</p><p className="text-[0.68rem] text-ink-faint">{o.title}</p></TD><TD className="text-ink-muted">{o.asset.code} · {o.asset.name}</TD><TD><Badge status={o.priority}>{o.priority}</Badge></TD><TD className="text-ink-muted">{o.scheduledStart?formatDate(o.scheduledStart):"Unscheduled"}<p className="text-[0.68rem] text-ink-faint">SLA {o.slaDueAt?formatDate(o.slaDueAt):"—"}</p></TD><TD className="text-ink-muted">{o.technicianName??"Unassigned"}</TD><TD>{formatCurrency(o.laborCost+o.materialCost)}</TD><TD>{o.downtimeHours.toFixed(1)}h</TD><TD><Badge status={o.status}>{o.status.replaceAll("_"," ")}</Badge></TD></TRow>)}{!orders.length&&<TRow><TD colSpan={8} className="py-12 text-center text-ink-faint">No asset work orders.</TD></TRow>}</TBody></Table></CardContent></Card></div>}
