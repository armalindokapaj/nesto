import{redirect}from"next/navigation";import{getCurrentUser}from"@/lib/dal";import{can}from"@/lib/permissions";import{getAssetsDashboard}from"@/server/assets-module";import{AssetsHeader,AssetsNav}from"@/components/assets/assets-nav";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{formatCurrency}from"@/lib/utils";
export default async function Page(){const{tenantId,role}=await getCurrentUser();if(!can(role,"PROJECTS","READ"))redirect("/dashboard/executive");const d=await getAssetsDashboard(tenantId);const byType=Object.entries(Object.groupBy(d.assets,a=>a.type));return <div className="space-y-6"><AssetsHeader title="Asset reports" description="Register, depreciation, maintenance cost, downtime, utilization, warranty, insurance, inspection and compliance reporting."/><AssetsNav active="reports"/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Metric label="Acquisition value" value={formatCurrency(d.acquisitionValue)}/><Metric label="Book value" value={formatCurrency(d.bookValue)}/><Metric label="Maintenance cost" value={formatCurrency(d.maintenanceCost)}/><Metric label="Downtime" value={`${d.downtimeHours.toFixed(1)} hours`}/></div><Card><CardHeader><CardTitle>Register by asset type</CardTitle></CardHeader><CardContent className="space-y-3">{byType.map(([type,rows])=><div key={type} className="flex items-center justify-between rounded-lg bg-surface-sunken p-3"><span className="text-sm text-ink-muted">{type.replaceAll("_"," ")}</span><b className="text-ink">{rows?.length??0}</b></div>)}</CardContent></Card></div>}
function Metric({label,value}:{label:string;value:string}){return <Card><CardContent className="pt-5"><p className="text-xs text-ink-muted">{label}</p><p className="mt-2 text-xl font-semibold text-ink">{value}</p></CardContent></Card>}
